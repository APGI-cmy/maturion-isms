# Functional Delivery Evidence — PR #1590

PR: #1590
Issue: #1589
Current head SHA reviewed: 9b24d7b2fe861386df31244e93c0844592323d5c
PROMISED_USER_JOURNEY: MMM Phase 6 end-to-end — organisation creation → Mode A/B/C framework creation → parse/AI generation → review page compile/publish → framework PUBLISHED → dashboard reflects telemetry for ADMIN/LEAD_AUDITOR role
ENTRY_POINT: /onboarding (OnboardingPage.tsx) for new org creation, or /frameworks/new (FrameworkUploadPage.tsx) for framework mode selection
FINAL_EXPECTED_STATE: Framework in PUBLISHED status; dashboard loads for ADMIN/LEAD_AUDITOR role and reflects parse/framework/AI telemetry accurately
USER_CAN_COMPLETE_JOURNEY: no — all code paths are implemented and CI is green; live deployed verification with authenticated ADMIN/LEAD_AUDITOR has not yet been executed (STOP_AND_FIX active)
Product/user journey: MMM onboarding authentication + organisation creation, framework mode selection, Mode A/B/C framework creation, review page compile/publish workflow, dashboard load
User journey tested: yes (code-level — all paths implemented; live verification pending deployed preview)
CTA_MAP: present
CTA/API map: present
BACKEND_CAPABILITY_MAP: present
Backend target proof: present
SCHEMA_CONTRACT_CHECK: present — all Edge Functions (mmm-framework-init, mmm-ai-framework-generate, mmm-ai-framework-parse, mmm-framework-compile, mmm-framework-publish, mmm-qiw-status, mmm-upload-framework-source) verified against actual migration schema; no invented columns
CROSS_FUNCTION_COMPATIBILITY_CHECK: present — mmm-upload-framework-source (ADMIN) triggers mmm-ai-framework-parse (ADMIN); compile chain reads proposed_domain_id/proposed_mps_id correctly; publish reads REVIEW status; role contracts aligned across all functions
ASYNC_JOB_CHECK: present — parse job transitions PENDING→PROCESSING→COMPLETE/FAILED; review page polls mmm_parse_jobs by framework_id every 4 s; Compile gated on COMPLETE status + proposed domain count > 0; FAILED state shown actionably
VISIBLE_STATE_CHECK: present — Compile button shows Compiling…/success banner/error alert; Publish button shows Publishing…/success banner/error alert; Publish disabled when framework.status ≠ review; parse job status rendered on review page
DEPLOYED_PREVIEW_CHECK: pending — Vercel preview deployed; authenticated ADMIN/LEAD_AUDITOR end-to-end workflow execution not yet performed
DASHBOARD_OR_STATE_REFLECTION_CHECK: pending — mmm-qiw-status error_rate fix committed (treats both 'completed'/'success' as success); live dashboard load with ADMIN/LEAD_AUDITOR role not yet verified on deployed preview
Screenshots or recording: present (Vercel preview)
Preview/live URL: https://maturity-model-management.vercel.app
Pass/fail result: partial-pass — all functional code fixes committed and CI gates green; end-to-end verification on deployed preview with authenticated ADMIN/LEAD_AUDITOR roles remains pending
KNOWN_PARTIALS: Dashboard live verification (ADMIN/LEAD_AUDITOR) pending; Mode A/B/C all code-level complete; live end-to-end proof not yet executed
Known partials:
- Dashboard requires valid ADMIN/LEAD_AUDITOR JWT and Supabase deployment — live verification pending
- Mode A: parse job polling + proposed-domain gate + compile/publish UI states ALL implemented at code level; live verification on deployed preview pending
- Mode B: AI generate → review page with proposed domains → compile → publish ALL implemented at code level; live verification pending
- Mode C: hybrid file upload (source_type: HYBRID) → parse job → review page polling ALL implemented at code level; live verification pending
Known limitations:
- Evidence is based on committed targeted tests, schema review, and deployed preview route checks
- Mode B/C AI generation depends on AIMC KUC_BASE_URL and AI inference availability in deployed environment
CS2_PARTIAL_ACCEPTANCE: yes
CS2_WAIVER_QUOTE: "That is acceptable from the admin/process side because this PR no longer claims full functional delivery. It preserves the distinction between administrative admissibility and functional completion. Admin/process position: APPROVED."
Partial scope accepted by CS2: yes — STOP_AND_FIX active
Builder QA functional report reference: modules/MMM/tests/B3-ui/b3-ui.test.ts
ECAP/admin-gate report reference: preflight/ecap-admin-ceremony
IAA final assurance reference: .agent-admin/assurance/iaa-wave-record-mmm-p4-phase6-stop-and-fix-20260510.md

ADMIN_PASS: yes
FUNCTIONAL_PASS: no
VERDICT: PARTIAL_FUNCTIONAL_DELIVERY
FULL_FUNCTIONAL_DELIVERY_VERDICT: PARTIAL_FUNCTIONAL_DELIVERY
STOP_AND_FIX: active — full functional closure requires deployed preview verification with authenticated ADMIN/LEAD_AUDITOR roles

## CTA/API Mapping Table (minimum)

| CTA / visible action | User intent | UI route/component | Backend/API/Edge target | Data/storage object | Success state | Failure state | Evidence |
|---|---|---|---|---|---|---|---|
| Continue onboarding | Create org and bootstrap membership | apps/mmm/src/pages/OnboardingPage.tsx | supabase.functions.invoke('mmm-org-create') → /api/organisations | mmm_organisations + mmm_profiles | navigates forward to framework flow | mutation error state rendered | modules/MMM/tests/B3-ui/b3-ui.test.ts |
| Start Mode A | Upload real framework source file | apps/mmm/src/pages/FrameworkUploadPage.tsx | mmm-framework-init → /api/frameworks/init → mmm-upload-framework-source → /api/upload/framework-source → mmm-ai-framework-parse (fire-and-forget) | mmm_frameworks + mmm_parse_jobs + mmm_proposed_domains/mps/criteria | navigate to /frameworks/:id/review; parse-job polling → COMPLETE; Compile enabled | visible actionable error message; parse job FAILED shown on review page | modules/MMM/tests/B3-ui/b3-ui.test.ts (T-MMM-S6-014, T-MMM-S6-UPL) |
| Start Mode B | AI-generate framework from scratch | apps/mmm/src/pages/FrameworkUploadPage.tsx | mmm-framework-init → /api/frameworks/init → mmm-ai-framework-generate | mmm_frameworks + mmm_proposed_domains/mps/criteria | navigate to /frameworks/:id/review; proposed domains exist immediately; Compile enabled | visible actionable error message | modules/MMM/tests/B3-ui/b3-ui.test.ts (T-MMM-S6-014) |
| Start Mode C | Hybrid upload + AI generation | apps/mmm/src/pages/FrameworkUploadPage.tsx | mmm-framework-init → /api/frameworks/init → mmm-upload-framework-source (source_type: HYBRID) → mmm-ai-framework-parse (fire-and-forget) | mmm_frameworks + mmm_parse_jobs + mmm_proposed_domains/mps/criteria | navigate to /frameworks/:id/review; parse-job polling → COMPLETE; Compile enabled | visible actionable error message; parse job FAILED shown on review page | modules/MMM/tests/B3-ui/b3-ui.test.ts (T-MMM-S6-014, T-MMM-S6-UPL) |
| Open dashboard | Load QIW status and summary state | apps/mmm/src/pages/DashboardPage.tsx | supabase.functions.invoke('mmm-qiw-status') → /api/qiw/status | mmm_ai_interactions + mmm_parse_jobs + mmm_assessments | status cards render; error_rate treats 'completed'/'success' as success | role/error state message shown | DashboardPage.tsx error/empty state tests |
| Compile | Compile proposed domains/MPS/criteria into canonical schema | apps/mmm/src/pages/FrameworkReviewPage.tsx | supabase.functions.invoke('mmm-framework-compile') | mmm_domains + mmm_maturity_process_steps + mmm_criteria | "✓ Framework compiled. Status moved to REVIEW." banner + Compile button disabled; cache invalidated | "✗ Compile failed: &lt;message&gt;" error banner with backend message | modules/MMM/tests/B3-ui/b3-ui.test.ts |
| Publish | Publish compiled framework (status must be REVIEW) | apps/mmm/src/pages/FrameworkReviewPage.tsx | supabase.functions.invoke('mmm-framework-publish') | mmm_frameworks + mmm_audit_logs | "✓ Framework published. Redirecting…" + navigate to /frameworks | "✗ Publish failed: &lt;message&gt;" error banner; Publish disabled unless status === REVIEW | modules/MMM/tests/B3-ui/b3-ui.test.ts |

## All code fixes committed this PR (code-level complete)

1. `mmm-framework-init`: removed non-existent `created_by` from mmm_frameworks insert; removed non-existent `actor_type`/`organisation_id`/`metadata` from mmm_audit_logs insert
2. `mmm-qiw-status`: fixed `mmm_ai_interactions` query to use actual columns (`action_type`, `duration_ms`, `status`); error_rate now treats both `'completed'` and `'success'` as success
3. `_shared/mmm-auth.ts`: CORS headers now included on all `jsonResponse` outputs, not just OPTIONS preflight
4. `FrameworkUploadPage.tsx`: Mode A navigates to `/frameworks/:id/review` on success; Mode B/C also navigate to `/frameworks/:id/review`; Mode C renders file input, uploads source via `mmm-upload-framework-source` with `source_type: 'HYBRID'`
5. `FrameworkReviewPage.tsx`: compile/publish CTAs wired to Supabase Edge Functions; loading/success/failure states; parse-job polling + proposed-domain Compile gate
6. `mmm-ai-framework-generate`: schema-aligned `mmm_ai_interactions` inserts + proposed-table inserts (`sort_order`, `source:'AI'`, `intent_statement`, `maturity_level_target`)
7. `mmm-ai-framework-parse`: schema-aligned `mmm_ai_interactions` + proposed-table inserts; reads `kuc_upload_id`/`kuc_parse_job_id`/`upload_metadata` from parse job `result_json` and forwards to AIMC; marks parse job `FAILED` on error
8. `mmm-upload-framework-source`: requires ADMIN role (aligned with `mmm-ai-framework-parse` ADMIN contract — prevents non-admin uploads creating parse jobs the parser then rejects with 403); fires `mmm-ai-framework-parse` (fire-and-forget, JWT forwarded); marks parse job `FAILED` on non-2xx/network error
9. `mmm-framework-compile`: proposed-table queries use `proposed_domain_id`/`proposed_mps_id`; `sort_order`; `intent_statement`; `maturity_level_target`
10. `mmm-framework-publish`: removed non-existent `published_at`; audit log uses `after_state`
11. `mmm_parse_jobs.framework_id` migration: `20260510000001_mmm_parse_jobs_framework_id.sql`
12. `modules/MMM/tests/B3-ui/b3-ui.test.ts`: T-MMM-S6-UPL test coverage for `/api/upload/framework-source` + ADMIN role contract assertion

## Route-to-Capability Proof (No Invented Endpoint)

| Route | Supabase Edge Function (capability) | Test/spec coverage | Implementation file |
|---|---|---|---|
| `/api/frameworks/init` | `mmm-framework-init` | `modules/MMM/tests/B3-ui/b3-ui.test.ts` (T-MMM-S6-014) | `supabase/functions/mmm-framework-init/index.ts` |
| `/api/qiw/status` | `mmm-qiw-status` | `modules/MMM/tests/B3-ui/b3-ui.test.ts` (T-MMM-S6-QIW) | `supabase/functions/mmm-qiw-status/index.ts` |
| `/api/upload/framework-source` | `mmm-upload-framework-source` | `modules/MMM/tests/B3-ui/b3-ui.test.ts` (T-MMM-S6-UPL) | `supabase/functions/mmm-upload-framework-source/index.ts` |

Route Approval: /api/frameworks/init → mmm-framework-init (Supabase Edge Function; route string in B3 test T-MMM-S6-014 and CTA map above)
Route Approval: /api/qiw/status → mmm-qiw-status (Supabase Edge Function; route string in B3 test T-MMM-S6-QIW and CTA map above)
Route Approval: /api/upload/framework-source → mmm-upload-framework-source (Supabase Edge Function; route string in B3 test T-MMM-S6-UPL and CTA map above)

## Gate-Change Validation Evidence

Changed gate/script files in this PR:
- `.github/scripts/validate-governance-evidence-exactness.sh`
- `.github/scripts/validate-governance-evidence-exactness.test.sh`
- `.github/workflows/polc-boundary-gate.yml`
- `.github/workflows/preflight-evidence-gate.yml`

Validation proof — Actions run 25628089130 on HEAD 615cd7b0038925a8eca479780774873b70e68b70:
- https://github.com/APGI-cmy/maturion-isms/actions/runs/25628089130
- `preflight/gate-changing-pr-rule` ✅ PASS (job 75226667121)
- `preflight/iaa-final-assurance` ✅ PASS (job 75226667143)
- `preflight/product-delivery-gates` ✅ PASS (job 75226667133)
- All 11 Preflight Evidence Gate jobs ✅ PASS
