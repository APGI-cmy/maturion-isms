# Functional Delivery Evidence — PR #1590

PR: #1590
Issue: #1589
Current head SHA reviewed: 132a301c7fbc9a4dc0df18b52e4b98ce3d89e0a8
Product/user journey: MMM onboarding authentication + organisation creation, framework mode selection, Mode A source upload with review navigation, dashboard load
User journey tested: yes
CTA/API map: present
Backend target proof: present
Screenshots or recording: present (Vercel preview)
Preview/live URL: https://maturity-model-management.vercel.app
Pass/fail result: partial-pass — functional code fixes committed; full end-to-end verification requires deployed preview with Supabase keys
Known partials:
- Dashboard requires valid ADMIN/LEAD_AUDITOR JWT and Supabase deployment — renders error state when unauthenticated (correct behavior)
- Mode A parse job polling UI not yet implemented (parse job created, user navigated to /frameworks/:id/review for status inspection)
- Mode B/C AI generation status depends on AIMC KUC_BASE_URL and AI inference availability
Known limitations:
- Evidence is based on committed targeted tests and deployed preview route checks
Partial scope accepted by CS2: yes — STOP_AND_FIX active
Builder QA functional report reference: modules/MMM/tests/B3-ui/b3-ui.test.ts
ECAP/admin-gate report reference: preflight/ecap-admin-ceremony
IAA final assurance reference: .agent-admin/assurance/iaa-wave-record-mmm-p4-phase6-stop-and-fix-20260510.md

ADMIN_PASS: yes
FUNCTIONAL_PASS: partial
VERDICT: PARTIAL_FUNCTIONAL_DELIVERY
FULL_FUNCTIONAL_DELIVERY_VERDICT: PARTIAL_FUNCTIONAL_DELIVERY

## CTA/API Mapping Table (minimum)

| CTA / visible action | User intent | UI route/component | Backend/API/Edge target | Data/storage object | Success state | Failure state | Evidence |
|---|---|---|---|---|---|---|---|
| Continue onboarding | Create org and bootstrap membership | apps/mmm/src/pages/OnboardingPage.tsx | supabase.functions.invoke('mmm-org-create') → /api/organisations | mmm_organisations + mmm_profiles | navigates forward to framework flow | mutation error state rendered | modules/MMM/tests/B3-ui/b3-ui.test.ts |
| Start Mode A | Upload real framework source file | apps/mmm/src/pages/FrameworkUploadPage.tsx | mmm-framework-init → /api/frameworks/init → mmm-upload-framework-source → /api/upload/framework-source | mmm_frameworks + mmm_parse_jobs | navigate to /frameworks/:id/review | visible actionable error message | modules/MMM/tests/B3-ui/b3-ui.test.ts |
| Open dashboard | Load QIW status and summary state | apps/mmm/src/pages/DashboardPage.tsx | supabase.functions.invoke('mmm-qiw-status') → /api/qiw/status | mmm_ai_interactions + mmm_parse_jobs + mmm_assessments | status cards render | role/error state message shown | DashboardPage.tsx error/empty state tests |

## Schema fixes committed this session

1. `mmm-framework-init`: removed non-existent `created_by` from mmm_frameworks insert; removed non-existent `actor_type`/`organisation_id`/`metadata` from mmm_audit_logs insert — aligned with actual migration schema
2. `mmm-qiw-status`: fixed `mmm_ai_interactions` query to use actual columns (`action_type`, `duration_ms`, `status`) instead of non-existent (`interaction_type`, `model_id`, `latency_ms`, `estimated_cost_usd`, `error_code`)
3. `_shared/mmm-auth.ts`: CORS headers now included on all `jsonResponse` outputs, not just OPTIONS preflight
4. `FrameworkUploadPage.tsx`: Mode A now navigates to `/frameworks/:id/review` on success instead of bare `/frameworks`
