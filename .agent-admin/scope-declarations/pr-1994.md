# PR #1994 Scope Declaration

PR: #1994
WAVE: PUBLIC-PROFILES-ROOT-PROVENANCE-RECOVERY-20260729
GOVERNING_ISSUE: #1993
BASE_BRANCH: main
STATUS: SOURCE_RECOVERY_DRAFT — AUTOMATION_EXCEPTION_RECORDED — QP_WITHHELD — ECAP_WITHHELD — IAA_WITHHELD — MERGE_NO_GO

## Objective

Recover exactly one committed, idempotent root migration for `public.profiles` so the shared ISMS foundation exists before recovered PIT Slice 4 source later creates `public.projects` with its profile foreign-key target.

## In scope

- `modules/mat/tests/foundational-provenance/public-profiles-root-migration.test.ts` — PR-specific source guard, intentionally RED before the recovery migration existed.
- `supabase/migrations/20260615000000_public_profiles_root_provenance.sql` — the only recovered database source.
- PR-specific scope, appointment, task, pre-brief, and source/live comparison records.

The recovered table contract is limited to 11 columns, the `auth.users(id)` and `public.organisations(id)` foreign keys, RLS, and the three self-only policies.

## Automatic preview exception

After the draft PR opened, the pre-existing repository-managed Supabase Git integration automatically created a non-production empty-data preview branch and processed the new migration. This was not operator-initiated, is not the authorised clean-bootstrap validation, and must not be used as QP, ECAP, IAA, or merge evidence.

Read-only confirmation: APGI-cmy’s Project does not record migration version `20260615000000`; production remains untouched.

## Explicit exclusions

- Applying SQL or migrations to APGI-cmy’s Project or any other manually targeted Supabase resource.
- Operator-initiated Supabase project or branch creation.
- Changing PR #1986, PR #1981, runtime/application code, workflows, deployment configuration, credentials, grants, service-role access, or RLS beyond this table’s three self-only policies.
- Restoring legacy MAT tables, triggers, storage, audits, domains, criteria, or product functionality.
- QP, ECAP, IAA, clean-bootstrap PASS, merge, or QA-resumption claims.

## Required later verification

A fresh disposable clean-bootstrap test may occur only after this PR’s source recovery and the automatic-preview exception are independently accepted. It must prove the complete reconciled inventory, not merely this migration, and must preserve proof that APGI-cmy’s Project was untouched.
