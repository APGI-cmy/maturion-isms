# PR #1952 Scope Declaration

| Field | Value |
|---|---|
| Issue | #1943 |
| PR | #1952 |
| Wave | `pit-stage12-slice4-supabase-project-persistence` |
| Builder | `pit-specialist` |
| Status | ACTIVE IMPLEMENTATION SCOPE — DRAFT PR |

## In scope

- Supabase-backed `projects` and `source_links` persistence.
- Existing authenticated actor, active organisation membership and role contract reuse.
- Organisation-scoped RLS and positive/negative policy verification.
- Stable asynchronous project repository interface.
- Project creation, register reads, authorised updates and `/projects/:id` overview.
- Deterministic loading, empty, not-found, denied and recoverable error states.
- Truthful browser-local compatibility notice without silent migration claims.
- QA-to-RED, RED-to-GREEN, deployment, browser and tracker evidence.

## Out of scope

Milestones, deliverables, tasks, dependencies, timeline, evidence uploads, approvals, notifications, reporting, roll-up, AIMC, complete lifecycle, full PIT, Stage 12 completion and production/release readiness.

## Change boundary

Expected changed areas:

- `apps/isms-portal/src/lib/` project repository, Supabase client and tests;
- `apps/isms-portal/src/pages/pit/` create, register and detail pages;
- `apps/isms-portal/src/App.tsx` and route constants;
- `apps/isms-portal/.env.example` and package dependency metadata;
- `supabase/migrations/` Slice 4 schema and RLS migration;
- `modules/pit/` governing alignment, implementation evidence and progress trackers;
- `.agent-admin/` PR-specific governance and delegation records.

No unrelated module runtime change is authorised.
