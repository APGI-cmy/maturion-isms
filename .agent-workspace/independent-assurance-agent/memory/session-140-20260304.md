# IAA Session Memory — session-140 — 2026-03-04

| Field | Value |
|---|---|
| `session_id` | session-140 |
| `date` | 2026-03-04 |
| `pr_reviewed` | copilot/fix-rls-policy-violations (wave postbuild-fails-03) |
| `invoking_agent` | foreman-v2-agent (session-102) |
| `producing_agent` | qa-builder (TASK-PBF3-004), schema-builder (TASK-PBF3-001), ui-builder (TASK-PBF3-002/003) |
| `producing_agent_class` | builder |
| `pr_category` | AAWP_MAT |
| `checks_executed` | 40 |
| `checks_passed` | 40 |
| `checks_failed` | 0 |
| `merge_gate_parity_result` | PASS |
| `verdict` | ASSURANCE-TOKEN |
| `token_reference` | IAA-session-140-wave-postbuild-fails-03-20260304-PASS |
| `adoption_phase_at_time_of_verdict` | PHASE_B_BLOCKING |
| `prior_sessions_reviewed` | session-135, session-136, session-138, session-139 |
| `re_invocation_context` | Re-invocation after session-139 REJECTION-PACKAGE (4 ceremony commits missing). All 4 failures remediated in commit ef641a6. |

---

## FAIL-ONLY-ONCE Rules Applied

| Rule | Outcome |
|------|---------|
| A-001: IAA invocation evidence present | PRESENT — Pre-Brief + session-139 rejection token on branch |
| A-002: No class exceptions | CONFIRMED — AAWP_MAT; not agent contract PR |
| A-021: Commit before invoking IAA | PASS — ef641a6 is HEAD; PREHANDOVER, session memory, SCOPE_DECLARATION.md, and hook changes all committed. REMEDIATED from session-139. |
| A-026: SCOPE_DECLARATION.md must match diff | PASS — BL-027 confirms 13/13 exact match. REMEDIATED from session-139. |
| A-029: PREHANDOVER immutability §4.3b | RESPECTED — PREHANDOVER committed at ef641a6 before IAA invocation; iaa_audit_token pre-populated as expected reference; IAA wrote dedicated token file; PREHANDOVER not edited |

**fail_only_once_updates**: None. All failures from session-139 are remediated. No new patterns to codify.

---

## Failures Cited

None. All 40 checks PASS.

---

## Carry-Forward Mandates

| CF ID | Description | Blocking? |
|---|---|---|
| CF-001 | Document domains/criteria/organisations DELETE RLS policy architectural decision in data-architecture.md (cascade-from-audit design; organisations admin-managed). Carried from session-139. | No |
| CF-002 | Add file type (MIME) and file size validation to useEvidence.ts file upload path. Pre-existing gap; useCriteria.ts already validates. | No |

---

## Substantive Assessment (90% IAA effort)

**Substantive work is SOUND. All delivery is production-ready.**

**Migration** (`20260305000000_fix_rls_current_setting_policies.sql`):
- Root cause correctly identified and fixed: `current_setting('app.current_organisation_id', true)` is never set by Supabase JS SDK → all authenticated SELECT/UPDATE/DELETE ops on audits/domains/criteria failed silently.
- Replacement: `auth.uid()` + profiles-JOIN subquery — standard Supabase org-isolation pattern ✅
- Idempotent throughout — safe to run on existing DB ✅
- audits: SELECT + UPDATE + DELETE all added. INSERT exists in prior migration (20260304000003). ✅
- domains/criteria: SELECT added. INSERT/UPDATE in prior migration (20260304000004). ✅
- organisations: SELECT added — was RLS-enabled but completely inaccessible. ✅

**useEvidence.ts**: Storage path org-prefixed (`${organisationId}/evidence/${criterionId}/...`). `organisation_id` and `created_by` added to INSERT payload. Auth gates and org membership guard applied. Error messages improved (split DB error vs org-not-linked). ✅

**useCriteria.ts**: Storage path org-prefixed (`${organisationId}/criteria/${auditId}/...`). File type + size validation present. Auth gates and org membership guard applied. ✅

**Tests**: T-PBF3-001 to T-PBF3-007 — meaningful file-content assertions with both positive (policy names, auth.uid()) and negative (no current_setting, no bare path) guards. MAT-T-0123 corrected `user_profiles` → `from('profiles')`. IAA independently verified all 8 tests GREEN with vitest.

---

## Suggestions for Improvement

Session-139 and session-140 together show the A-021 pattern recurring (ceremony artifacts committed in a separate late commit rather than being part of the original PR work). The Foreman Pre-IAA Commit Gate recommendation from session-139 remains valid. In this session-140 invocation, all items were properly committed before IAA was called — demonstrating the workflow is fixable. Recommendation: the Foreman PREHANDOVER template should include a mandatory `git log --oneline -1` evidence block showing the PREHANDOVER file itself as the most recent (or second-most recent) commit on the branch, confirming it was committed before handover was signed and before IAA was invoked. This makes the commit requirement explicit and verifiable in the template itself rather than relying on process memory.

---

## Parking Station Entry

| Date | Agent | Session | Phase | Summary | Session File |
|---|---|---|---|---|---|
| 2026-03-04 | independent-assurance-agent | session-140 | Phase 4 | ASSURANCE-TOKEN issued — all 40 checks PASS. A-021 remediation confirmed. Carry-forward CF-002: add file validation to useEvidence.ts next wave. | session-140-20260304.md |
