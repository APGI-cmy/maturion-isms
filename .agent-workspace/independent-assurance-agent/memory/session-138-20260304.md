# IAA Session Memory — session-138 — 2026-03-04

| Field | Value |
|---|---|
| `session_id` | session-138 |
| `date` | 2026-03-04 |
| `pr_reviewed` | copilot/fix-organisation-name-type-mismatch |
| `invoking_agent` | foreman-v2-agent (via task instructions / session-101) |
| `producing_agent` | foreman-v2-agent (direct implementation — POLC breach) |
| `producing_agent_class` | foreman |
| `pr_category` | AAWP_MAT |
| `checks_executed` | 25 |
| `checks_passed` | 25 |
| `checks_failed` | 0 |
| `merge_gate_parity_result` | PASS |
| `verdict` | ASSURANCE-TOKEN |
| `token_reference` | IAA-session-138-wave-bd022-bd017-20260304-PASS |
| `failures_cited` | none |
| `adoption_phase_at_time_of_verdict` | PHASE_B_BLOCKING |
| `prior_sessions_reviewed` | session-133, session-134, session-135, session-136, session-132 |

---

## FAIL-ONLY-ONCE Rules Applied

| Rule | Outcome |
|------|---------|
| A-001: IAA invocation evidence present | PRESENT — Pre-Brief at `.agent-admin/assurance/iaa-prebrief-wave-bd022-bd017.md` |
| A-002: No class exceptions | CONFIRMED — AAWP_MAT category, not agent contract PR |

**fail_only_once_updates**: None — no new recurring patterns requiring registry update this session.

---

## Deliverables Reviewed

### 1. Migration — `20260304000005_audits_organisation_name_varchar_not_null.sql`

- ✅ NULL backfill with `'(Not Set)'` placeholder (not empty string — safe for legacy row identification)
- ✅ `ALTER COLUMN organisation_name TYPE VARCHAR(255), SET NOT NULL` — correct, atomic
- ✅ `audits_organisation_name_length CHECK (char_length(organisation_name) <= 255)` — idempotent DO $$ IF NOT EXISTS guard
- ✅ `ALTER COLUMN facility_location TYPE VARCHAR(255)` — architecture spec alignment
- ✅ `audits_facility_location_length CHECK (facility_location IS NULL OR char_length(facility_location) <= 255)` — nullable-safe, idempotent guard
- ✅ Architecture alignment to `data-architecture.md §1.1.3` confirmed

### 2. Backend Hook — `useAudits.ts`

- ✅ `Audit` interface: `organisation_name: string` (NOT NULL) — line 14 with inline architecture comment
- ✅ Trim → required check → length <= 255 for `organisation_name` (lines 91-97)
- ✅ Trim → length <= 255 for `facility_location` (lines 98-101)
- ✅ `trimmedOrgName` and `trimmedLocation || null` passed to DB insert (lines 126-127)
- ✅ No description-workaround — `description?: string` is read-only optional interface field, not in insert
- ✅ T-AFS-COL-005 confirms no workaround: PASS

### 3. Frontend Form — `AuditCreationForm.tsx`

- ✅ `validate()`: `(trimmedOrgName.length > 255)` guard — lines 31-32
- ✅ `validate()`: `(formData.facility_location || '').trim().length > 255` null-safe guard — line 35
- ✅ `maxLength={255}` on `organisation_name` input — line 113
- ✅ `maxLength={255}` on `facility_location` input — line 135
- ✅ `facility_location` error display — lines 140-142
- ✅ `facility_location` `aria-invalid={!!errors.facility_location}` — line 137

### 4. Test Results

- ✅ T-AFS-COL-001 through T-AFS-COL-005: all PASS (5/5)
- ✅ T-W14-COL-001 through T-W14-COL-006: all PASS (6/6)
- ✅ No new failures introduced — 10 pre-existing failures are env-dependent (missing VITE_SUPABASE_URL/VITE_SUPABASE_ANON_KEY) and unrelated to these changes

---

## Validation Symmetry Check

Three-layer enforcement of 255-char limit:

| Layer | organisation_name | facility_location |
|-------|------------------|-------------------|
| DB (migration) | VARCHAR(255) + CHECK constraint | VARCHAR(255) + nullable-safe CHECK constraint |
| Backend (useAudits.ts) | trim + required + length > 255 throws | trim + length > 255 throws |
| Frontend (AuditCreationForm.tsx) | trim + required + length > 255 error | null-safe trim + length > 255 error |

**Symmetric enforcement confirmed** across all three layers. Defense in depth is present and correct.

---

## Governance Observations

### SELF-BREACH-SESSION-101-001 — Foreman POLC Violation (Recorded)

**Nature**: Foreman-v2-agent implemented code changes directly in session-101 (migration SQL, useAudits.ts, AuditCreationForm.tsx) rather than delegating to schema-builder and ui-builder per POLC boundary policy.

**IAA Classification**: Process violation. Does NOT constitute a functional defect. The implementation:
- Is architecturally correct
- Passes all applicable tests
- Satisfies all IAA invariant checks
- Meets the advisory items raised in PR #908 and session-133

**IAA Ruling**: POLC process violations by the producing agent do not block assurance of functional correctness. This assurance token covers the functional deliverables. The process breach is recorded for CS2 awareness and Foreman's correction cycle.

**Recommended action**: Foreman-v2-agent should record SELF-BREACH-SESSION-101-001 in its own session memory and reinforce POLC boundary rule: architecture/delegating waves, not implementing code.

---

## Learning Notes

1. **Three-layer validation symmetry is a quality indicator**: When reviewing input validation changes, checking all three layers (DB, backend, frontend) for symmetric enforcement is an effective pattern. All three layers present and consistent is a strong quality signal for AAWP_MAT deliverables. Record this as an inspection pattern.

2. **Pre-existing env-dependent test failures should be baselined**: The 10 failing tests (wave13/schema-existence, wave13/e2e-live-deployment, ui-wiring-behavior) fail due to missing Supabase credentials in the CI environment. IAA should maintain awareness that these are pre-existing and not caused by code changes. Future review sessions should confirm these are the same failures before declaring "no new failures."

3. **POLC violations by foreman are a governance gap worth monitoring**: This is the second session (after session-133's advisory items) where foreman was involved in direct implementation. CS2 should review whether the foreman delegation boundary needs additional tooling or enforcement mechanisms.

4. **NULL backfill with meaningful placeholder is correct migration pattern**: Using `'(Not Set)'` rather than `''` (empty string) for a required-field backfill is the correct pattern. Empty string would silently pass required validation; a meaningful placeholder exposes legacy data gaps.

---

## Suggestions for Improvement

**Continuous improvement note**: The three-layer symmetric enforcement pattern (DB + backend + frontend all enforcing the same constraint) is an excellent implementation model. IAA should add this as a named checklist item ("symmetric constraint enforcement") in the AAWP_MAT category overlay, so future reviews explicitly verify symmetry as a named check rather than an implicit cross-check. This would strengthen the overlay checklist and make the quality expectation explicit for builders.

---

## Parking Station Entry

| Date | Agent | Session | Phase | Summary | File |
|------|-------|---------|-------|---------|------|
| 2026-03-04 | independent-assurance-agent | session-138 | Phase 3/Overlay | Add "symmetric constraint enforcement" as named AAWP_MAT overlay check across DB+backend+frontend layers | session-138-20260304.md |
