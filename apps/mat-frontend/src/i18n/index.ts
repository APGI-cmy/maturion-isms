// i18n configuration stub
export const defaultLocale = 'en';

export const translations = {
  en: {
    common: {
      welcome: 'Welcome to MAT',
      logout: 'Logout',
    },
  },
};

export function t(key: string): string {
  return key;
}
